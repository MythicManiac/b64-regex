[tool.poetry]
name = "b64-regex"
version = "0.1.0"
description = ""
authors = ["Mythic <mythicmaniac@gmail.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "b64_regex"}]

[tool.poetry.dependencies]
python = "^3.11"
pytest = "^7.4.0"
bitarray = "^2.7.6"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
